import styled from 'styled-components';

const SideNav = () => {
  return <SideNavContainer>SideNav</SideNavContainer>;
};

const SideNavContainer = styled.nav`
  width: 164px;
  background-color: azure;
`;

export default SideNav;
